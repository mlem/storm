# Options mod for Storm

Enables adding and using options from any storm mod.
Adds a `Storm` Tab to options menu in PZ.

## Build instructions

```shell
gradlew storm-options:prepareDistribution dist
```